describe('Header', () => {
  it('should pass basic test', () => {
    expect(1 + 1).toBe(2);
  });

  it('should validate string operations', () => {
    expect('Benjamin Grauer').toContain('Benjamin');
  });
});